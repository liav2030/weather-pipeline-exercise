name: Runner Test (GH-hosted)

on:
  push:
    branches: [ "**" ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Print env
        run: |
          echo "✅ Running on GitHub-hosted runner"
          echo "OS: $RUNNER_OS"
          echo "Shell: $0"
          python3 --version
          docker --version
